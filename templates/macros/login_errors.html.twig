{% macro render_error(error) %}
    {% if error %}
        <div class="mt-1 mb-4 text-sm text-red-600 bg-red-100 border border-red-300 rounded px-3 py-2">
            {{ error }}
        </div>
    {% endif %}
{% endmacro %}
