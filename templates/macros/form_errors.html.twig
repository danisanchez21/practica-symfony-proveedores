{% macro render_errors(errors) %}
    {% if errors %}
        <div class="mt-1 text-sm text-red-600 bg-red-100 border border-red-300 rounded px-3 py-2">
            {{ errors|raw }}     {# Con el RAW evito que lo escape como texto#}
        </div>
    {% endif %}
{% endmacro %}