<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Proveedores{% endblock %}</title>
    <link rel="icon" type="image/png" href="{{ asset('logo-tab.jpeg') }}" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
      {{ encore_entry_link_tags('app') }}
</head>
<body class="bg-gray-200 text-gray-900 font-sans">
<header 
  class="h-20 shadow flex items-center justify-between px-6"
  style="background-color: #7a2f2f; background-image: radial-gradient(at 60% 30%, hsl(0, 80%, 30%) 0, transparent 60%), radial-gradient(at 10% 40%, hsl(0, 70%, 30%) 0, transparent 60%);">
    <div class="container mx-auto flex justify-between items-center p-4">
        <h1 class="text-xl md:text-2xl font-semibold text-white">Gestión de Proveedores</h1>
        <img src="{{ asset('logo-viajes.png') }}" alt="Viajes Para Ti" class="h-10 ml-auto mr-4">
    {% if app.user %}
            <a href="{{ path('app_logout') }}" 
               class="text-white border border-white rounded px-3 py-1 text-sm hover:bg-white hover:text-red-700 transition">
               Cerrar sesión
            </a>
        {% endif %}
    </div>
  </div>
</header>

<main class="container mx-auto py-8">
    {% block panel_content %}{% endblock %}
</main>

<footer class="text-center text-sm text-gray-500 py-4">
    &copy; {{ "now"|date("Y") }} Viajes Para Ti
</footer>

</body>
</html>
